[package]
name = "vigil"
version = "0.2.3"
edition = "2021"
publish = false
description = "Supply-chain dormancy scanner"
authors = ["studio2201"]
license = "Apache-2.0"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
panic = "abort"
